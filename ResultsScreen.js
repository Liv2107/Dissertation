import PhotoPreview from './PhotoPreview.js';

import React from 'react';
import { Text, TouchableOpacity, SafeAreaView, Image, View, StyleSheet } from 'react-native';

const ResultsScreen = ({photos}) => {

    // photos - given IDs of positions of the selected products.
    // search these products and display thier name, shade, brand, image, cost, and link. - extract from another database.

    return (
        <SafeAreaView>

        </SafeAreaView>
    );

    // later tasks include:
    // filtering the products, eg  only Maybeline, products that cost less then $10.
}